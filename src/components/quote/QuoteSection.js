import React, { Component } from "react";
import styles from "./quote-section.mobule.css";
export default class QuoteSection extends Component {
  render() {
    return <section className={styles["quote-section-container"]}></section>;
  }
}
